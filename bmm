#include<iostream>
using namespace std;

int main()
{
    int n1,n2,big;

    cin>>n1;
    cin>>n2;
    n1>n2 ? big=n1;
    big=n2;
    int max = -1;
    for ( int i=1; i<=big ; i++
    {
        if( n1%i==0 && n2%i==0 && i>max )
        {
            max=i;
        }
    }
    cout << 'bmm:' << max;
    
    return0;
}
